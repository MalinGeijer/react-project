1. Hur din nuvarande struktur mappar mot projektkraven

Din struktur:

.
├── app.py              ← Flask API (root)
├── data/               ← rådata + rensad data
├── notebooks/          ← EDA, experiment, analys
├── report.txt          ← slutsatser & jämförelse
├── requirements.txt   ← beroenden
└── src/
    ├── data_processing.py
    ├── model_training.py
    ├── utils.py
    └── main.py


Detta motsvarar exakt kursmomenten:

✅ 1. Databehandling & EDA
Kurskrav	Din mapp
Läs in dataset	data/ + data_processing.py
Datarensning	data_processing.py
EDA + visualisering	notebooks/

✅ Helt rätt att lägga EDA i notebooks – det är akademisk standard.

✅ 2. Modellering
Kurskrav	Din fil
Träning	model_training.py
Train/test split	model_training.py
Utvärdering	model_training.py + ev notebook
✅ 3. Modelljämförelse & rapport
Krav	Din lösning
Jämförelse	report.txt
Motivering	report.txt

Det här är exakt så handledare vill se det:
kod ≠ analys ≠ rapport.

2. Din Flask-server i root – helt rätt tänkt

Det du har i root app.py just nu är perfekt som:

✅ Senare inferens-API
✅ Kommunikationsbrygga mellan Python & React
✅ Isolerad från träningskoden

Det är väldigt viktigt att:

Träning ≠ API-server

Analys ≠ Produktion

Du har redan gjort den separationen korrekt.

3. Din UI-idé: Terminal / Tkinter / Streamlit

Här är hur du ska tänka strategiskt:

Val	När passar det?	Koppling till React senare
Terminal	Förstå flödet logiskt	✅ Enkelt att ersätta
Tkinter	Visa att du kan GUI i Python	❌ Går ej att återanvända
Streamlit	Portfolio, demo, snabb prototyp	✅ Mycket bra brygga
Min rekommendation i ditt fall:

Börja med Terminal → gå över till Streamlit → ersätt med React

För då får du:

Full logikförståelse

Snabb visuell feedback

Enkel React-migrering

4. Hur din app-logik bör flöda (utan kod)
Terminal / Streamlit-version (lokal AI-app):
Starta program
↓
Ladda tränad modell från fil
↓
Användaren matar in data (eller laddar bild)
↓
Datat förbehandlas
↓
Modell predikterar
↓
Resultat visas

Flask / React-version (senare):
React skickar data
↓
Flask tar emot POST
↓
Förbehandling
↓
Modell predikterar
↓
JSON-svar tillbaka till React
↓
Resultat visas i UI


Det är samma pipeline – bara olika input/output.
Detta är exakt sådan arkitektur man vill se i seriösa ML-projekt.

5. Mycket viktig observation (du tänker helt rätt redan)

Det här du skrev:

“app.py under src kommer att flytta till frontend react senare”

Det är helt korrekt tänkt i koncept, men viktigt att tänka så här:

🧠 Logik stannar i Python

🎨 Presentation flyttar till React

🔌 Flask blir bara ett gränssnitt (API)

Alltså:

data_processing.py → stannar

model_training.py → stannar

Flask app.py → stannar

Endast UI-delen försvinner ur Python

6. Hur detta uppfyller “Jag vill förstå”

Du bygger nu:

✅ Vetenskaplig ML-pipeline
✅ Riktig datarensning
✅ Modelljämförelse
✅ Tolkning
✅ Frontend-integration
✅ Produktionsliknande API

Detta är långt över “skolnivå” – detta är faktiskt junior ML-engineer-nivå i struktur.


Vad är EDA?

EDA = Exploratory Data Analysis
På svenska: utforskande dataanalys.

Det är inte modellträning.

Det är inte avancerad statistik.
Det är en fas där du som människa försöker förstå ditt data innan du bygger en modell.

Det är alltså:

“Låt mig kolla vad jag faktiskt jobbar med innan jag tränar en modell.”

Varför gör man EDA?

För att:

hitta felaktigheter i data (saknade värden, konstiga tal)

se mönster (korrelationer, fördelningar)

se om klasser är jämnt fördelade

få en känsla för “hur svårt problemet är”

bestämma vilken modelltyp som passar

Det är både visuell och statistisk utforskning.

Varför ligger EDA i notebooks/?

För att:

Jupyter Notebook passar perfekt för diagram, utforskning, testkod.

Man vill hålla det separerat från produktionskoden.

Det blir läsbart för handledare och examinatorer.

Akademisk standard är:

notebooks/
    01_exploration.ipynb
    02_cleaning.ipynb
    03_model_tests.ipynb


Där du gör:

printar head()

gör grafer

testar olika preprocessing-idéer

testar vilka features som verkar viktiga

Det är alltså din anteckningsbok för att förstå datat.

Konkreta exempel på vad EDA innehåller

Om du har ett tabell-dataset:

1. Snabb inspektion

df.head()

df.info()

df.describe()

2. Saknade värden

df.isna().sum()

3. Klassfördelning (om klassificering)

df['label'].value_counts()

4. Korrelationsmatris

sns.heatmap(df.corr())

5. Histogram

df['age'].hist()

6. Boxplots (outliers)

sns.boxplot(data=df, x='age')

Om du jobbar med MNIST (bilder)

EDA blir t.ex:

1. Visa några exempelbilder
2. Räkna hur många av varje siffra finns
3. Visa medelbilden för varje siffra
4. Titta på pixelvärdenas distribution
5. Visualisera bildens intensitet

Syftet är att se:

är siffrorna centrerade?

är vissa siffror lättare än andra?

verkar datat rent?

EDA i en mening

EDA är ditt sätt att förstå datat innan du ger det till en modell.
Det är utforskning, inte produktion.
Och därför gör man det i en notebook.

project-root/
├── data/              ← rensad, sparad, kontrollerad data
├── notebooks/        ← EDA + analysis.ipynb
├── src/               ← träningskod, preprocessing, utils
├── models/            ← sparade modeller (.h5, .pkl, .onnx)
├── app.py             ← Flask
└── requirements.txt

tensorflow → Keras + MNIST + framtida modellträning

jupyter → för att köra analysis.ipynb

matplotlib → grundläggande diagram

seaborn → snyggare EDA-visualiseringar

pandas → datahantering (även om MNIST är bilder)

scikit-learn → train/test split, metrics, alternativa modeller
